const mongoose = require("mongoose");
const Schema = mongoose.Schema;
// definimos el esquema de la colección
const studentSchema = new Schema({
    firstName:{
        type: String
        required: [true, "Fisrt name is requiered"],
    }

    firstName:{

    }
}
    
);
  